Check the status of a task subagent.

Use this after launching `task` with `background: true`, after interruptions, or when resuming a long operation. Set `wait` to true when you need the result before continuing. A `stale` result means the task was persisted as running, but the process no longer owns the running fiber; restart or replace that lane before trusting the operation state.
