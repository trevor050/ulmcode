List background tasks known to the current project.

Use this after compaction, reloads, interruptions, or long unattended runs when task IDs may have been lost. Jobs persisted as `running` without an active fiber are reported as `stale`. Poll a specific task with `task_status` once you identify the task ID.
